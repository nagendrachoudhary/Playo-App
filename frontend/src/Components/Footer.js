import { Box } from '@chakra-ui/react';
import React from 'react';

function Footer(props) {
    return (
        <div>
            <Box bg={'lightcoral'} width={'100%'} height={'100px'} alignItems={'center'} display={'flex'} justifyContent={'space-between'} >

            </Box>
        </div>
    );
}

export default Footer;